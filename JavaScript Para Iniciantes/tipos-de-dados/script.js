// Declare uma variável contendo ma string
var nome = "Ícaro";

// Declare uma váriavel contendo um número dentro de uma string
var anos = "19";

// Declare uma variável com sua idade
var idade = 19;

// Declare duas varáveis, uma com seu nome e outra com seu sobrenome e some os mesmos
var sobrenome = "Vieira"
 
var nomecompleto1 = nome + ' ' + sobrenome; // Gambiarra
var nomecompleto = `${nome} ${sobrenome}`;

console.log(nomecompleto)

// Coloque a seguinte frase em uma variável: It's time
var frase = "It's time";

// Verifique o tipo da variável que contém o seu nome
var tipoDeVariavel = typeof nome;
console.log(tipoDeVariavel)